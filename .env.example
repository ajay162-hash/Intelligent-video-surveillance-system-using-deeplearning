# Example environment file for the app
# Copy this to `.env` at the project root and fill in values.

# --- Twilio WhatsApp (optional) ---
# SID and token from https://www.twilio.com/console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Your WhatsApp-enabled sender. Formats accepted:
# - Full WhatsApp format: whatsapp:+1415XXXXXXX
# - Or E.164 number: +1415XXXXXXX (the app will add whatsapp:)
TWILIO_FROM_NUMBER=

# --- Gemini (Google Generative AI) (optional) ---
# API key from Google AI Studio: https://aistudio.google.com/
GOOGLE_API_KEY=
# Model name; defaults to gemini-2.0-flash if omitted
GEMINI_MODEL=gemini-2.0-flash

# --- Media publishing for WhatsApp alerts (choose ONE path) ---
# 1) Public base URL serving static/uploads (must be HTTPS, public)
#    Example: https://your-domain.example.com/static/uploads
MEDIA_BASE_URL=

# 2) Supabase Storage (recommended). Requires a project and a public bucket.
#    Create a bucket (default name `media`) and a service role key.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=media
